import { Copy, KeyRound, ShieldCheck, Terminal } from "lucide-react";
import { Button } from "@/components/ui/button";

const copy = (value: string) => navigator.clipboard?.writeText(value);

export default function PublicInstall() {
  return <main className="min-h-screen bg-[#080b10] px-5 py-10 text-slate-100 sm:px-10"><div className="mx-auto max-w-5xl"><div className="mb-16 flex items-center justify-between"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-cyan-300 text-slate-950"><Terminal className="h-5 w-5"/></span><span className="font-mono text-sm font-bold tracking-[0.24em] text-cyan-200">CO08.ART</span></div><Button asChild className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><a href="/dashboard">Open dashboard</a></Button></div><section className="max-w-3xl"><p className="mb-4 font-mono text-xs uppercase tracking-[0.3em] text-amber-200">SSH hostname control</p><h1 className="text-4xl font-semibold tracking-tight text-white sm:text-6xl">One command. One clean SSH name.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-slate-400">Install the tiny CO08 client on your VPS, run <span className="font-mono text-cyan-200">co08 ssh</span>, and get a DNS-only hostname such as <span className="font-mono text-cyan-200">ip-899ac5a0.co08.art</span>.</p></section><div className="mt-12 grid gap-5 md:grid-cols-3"><div className="rounded-2xl border border-white/10 bg-white/[0.04] p-6"><KeyRound className="mb-5 text-cyan-200"/><h2 className="font-semibold text-white">1. Generate a token</h2><p className="mt-2 text-sm leading-6 text-slate-400">Open the dashboard and create a short-lived one-time VPS command.</p></div><div className="rounded-2xl border border-white/10 bg-white/[0.04] p-6"><Terminal className="mb-5 text-cyan-200"/><h2 className="font-semibold text-white">2. Install and run</h2><p className="mt-2 text-sm leading-6 text-slate-400">On the VPS, install the CLI and run it with the generated token.</p></div><div className="rounded-2xl border border-white/10 bg-white/[0.04] p-6"><ShieldCheck className="mb-5 text-emerald-300"/><h2 className="font-semibold text-white">3. Connect safely</h2><p className="mt-2 text-sm leading-6 text-slate-400">The app never opens SSH or changes passwords. It only provisions DNS and prints your command.</p></div></div><section className="mt-10 rounded-2xl border border-cyan-300/20 bg-cyan-300/[0.05] p-6"><div className="flex items-center justify-between gap-4"><div><h2 className="font-semibold text-white">Install from the repository</h2><p className="mt-2 text-sm text-slate-400">Use Python 3.9+ and a user-provided token.</p></div><Button size="icon" variant="outline" className="border-white/10 bg-transparent" onClick={() => copy("python3 -m pip install git+https://github.com/replitprivet-dotcom/co08-ssh-hosts.git#subdirectory=cli")}><Copy className="h-4 w-4"/></Button></div><code className="mt-5 block overflow-x-auto rounded-xl bg-black/30 p-4 font-mono text-sm text-cyan-100">python3 -m pip install git+https://github.com/replitprivet-dotcom/co08-ssh-hosts.git#subdirectory=cli</code><code className="mt-3 block overflow-x-auto rounded-xl bg-black/30 p-4 font-mono text-sm text-cyan-100">CO08_SERVER=https://co08.art CO08_TOKEN=your_one_time_token co08 ssh --user root --port 22</code></section><section className="mt-8 rounded-2xl border border-white/10 bg-white/[0.04] p-6"><h2 className="font-semibold text-white">SSH key setup</h2><p className="mt-2 text-sm leading-6 text-slate-400">Create a key locally with <span className="font-mono text-cyan-200">ssh-keygen -t ed25519</span>, then add the public key to the VPS account’s <span className="font-mono text-cyan-200">~/.ssh/authorized_keys</span> using your provider console or an existing secure session. Never paste the private key into CO08. Password login is optional and should be temporary only; use a strong unique password, disable password authentication after key setup, and never send that password to this service.</p></section></div></main>; }
