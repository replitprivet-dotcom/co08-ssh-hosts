[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "co08"
version = "0.1.0"
description = "CO08 SSH hostname client"
requires-python = ">=3.9"

[project.scripts]
co08 = "co08_cli:main"
